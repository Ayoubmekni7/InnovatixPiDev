framework:
  http_client:
    scoped_clients:
      yousign.client:
        base_uri: 'https://api-sandbox.yousign.app/v3/'
        headers:
          Accept: 'application/json'
          Authorization: 'Bearer %YOUSIGN_SANDBOX_API_KEY%'